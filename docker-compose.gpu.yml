services:
  ollama:
    # GPU-specific overrides applied only when this file is included.
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
